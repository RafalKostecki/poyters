@import '../../styles.styl'

.footer
    width calc(100% - 60px)
    padding 30px
    float left
    margin-top 15px
    &--style
        background backgroundColor1
        color inherit
        text-align center
        txt(15px, 0px, 400, none, inherit)