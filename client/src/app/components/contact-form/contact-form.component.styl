@import '../../../assets/styles/index.styl'

.contactForm {
  display: flex
  flex-direction: column

  &__subInput, &__horizontalInput, &__content {
    margin: 8px 0
    padding: 12px 10px

    background-color: shadow4
    color: fontColor4
    border: none  
    border-radius: 2px
  }

  &__content {
    display: flex;
  }
}